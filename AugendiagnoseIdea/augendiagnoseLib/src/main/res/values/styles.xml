<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Holo">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>

    <style name="Divider">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">2dp</item>
        <item name="android:background">?android:attr/listDivider</item>
    </style>

    <style name="Divider_Vertical">
        <item name="android:layout_width">2dp</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:background">?android:attr/listDivider</item>
    </style>

    <style name="Space">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">0dp</item>
        <item name="android:layout_weight">1</item>
    </style>

    <style name="Subactivity">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:paddingLeft">@dimen/activity_horizontal_margin</item>
        <item name="android:paddingRight">@dimen/activity_horizontal_margin</item>
        <item name="android:orientation">vertical</item>
    </style>

    <!-- Styles for creating LinearLayout like action bar -->

    <style name="ActionBarButton">
        <item name="android:background">@drawable/actionbutton</item>
        <item name="android:ellipsize">end</item>
        <item name="android:singleLine">true</item>
        <item name="android:textSize">0px</item>
        <item name="android:text"/>
    </style>

    <style name="ActionBarButtonPortrait" parent="ActionBarButton">
        <item name="android:layout_width">@dimen/actionbar_buttonwidth</item>
        <item name="android:layout_height">@dimen/actionbar_height</item>
    </style>

    <style name="ActionBarButtonLandscape" parent="ActionBarButton">
        <item name="android:layout_height">@dimen/actionbar_buttonwidth</item>
        <item name="android:layout_width">@dimen/actionbar_height</item>
    </style>

    <style name="SeparatorBar">
        <item name="android:background">@android:color/holo_blue_dark</item>
    </style>

    <style name="SeparatorBarHorizontal" parent="SeparatorBar">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">@dimen/actionbar_colorstrip</item>
    </style>

    <style name="SeparatorBarVertical" parent="SeparatorBar">
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_width">@dimen/actionbar_colorstrip</item>
    </style>

</resources>